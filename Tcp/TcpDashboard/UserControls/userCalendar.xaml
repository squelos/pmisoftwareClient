<UserControl x:Class="TcpDashboard.UserControls.userCalendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="480">
    <GroupBox Header="Calendrier">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Vertical">
                <ComboBox/>
                <StackPanel>
                    <WrapPanel>
                        <ToggleButton Content="Jour" Margin="10,10,10,10"></ToggleButton>
                        <ToggleButton Content="Semaine" Margin="10,10,10,10"></ToggleButton>
                    </WrapPanel>
                    <Calendar  FontStretch="UltraExpanded" FontSize="21.333" FontWeight="Bold"/>
                </StackPanel>
                <DockPanel>
                   
                </DockPanel>
            </StackPanel>
        </Grid>
    </GroupBox>
</UserControl>
